# 规则回测

适用于按配置重建离线规则结果，或基于已存在的拒绝码复盘规则表现。

输入规则配置必须通过 `schemas/rule_config.schema.json`；规则顺序必须固定。输出样本级命中明细、总体汇总、单规则独立汇总和拒绝码一致性检查。

校验：样本级结果行数等于输入行数；拒绝码属于配置规则；单规则满足 `sample_cnt - hit_cnt = pass_cnt`；所有样本量均按确认的 `analysis_unit_id` 计算。
